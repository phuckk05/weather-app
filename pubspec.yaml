name: weather_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter



  cupertino_icons: ^1.0.8
   #thêm google fonts
  google_fonts: ^6.2.1

  #thêm thư viện bottom nav bar 
  salomon_bottom_bar: ^3.3.2

  #thêm thư viện loading_animation_widget
  loading_animation_widget: ^1.3.0
  intl: ^0.20.2
  scrollable_positioned_list: ^0.3.8

  #state management
  flutter_bloc: ^9.1.1

  #thêm thư viện svg
  flutter_svg: ^2.2.0

  #thêm thư viện responsive framework
  responsive_framework: ^1.5.1

  google_nav_bar: ^5.0.7
  month_year_picker: ^0.5.0+1


  #thêm thư viện dùng sidebar
  easy_sidemenu: ^0.6.1

  #thêm thưc viện cuộn đẹp
  web_smooth_scroll: ^1.1.0

  #thêm animation
  animations: ^2.1.1

  #thêm routes
  go_router: ^16.1.0

  #thêm thư viện mở link
  url_launcher: ^6.3.0

  #thêm 
  flutter_dotenv: ^6.0.0
  firebase_core: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0

flutter:

assets:
  - assets/images/
  - .env

  uses-material-design: true

